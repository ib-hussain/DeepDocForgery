# Setup logs

`setup/cpu.sh` and `setup/cuda.sh` write complete timestamped installation,
dependency-check, and test transcripts here. These generated logs are local
runtime artefacts and are not included in code-only releases.
