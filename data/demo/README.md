# DeepDocForgery demo data

These synthetic documents and masks are generated entirely by `scripts.make_demo_dataset`; they contain no external images or personal data. They exist only for pipeline tests and must not be used as scientific evidence.
